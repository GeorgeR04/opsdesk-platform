apiVersion: v1
kind: Service
metadata:
  name: opsdesk-api
  namespace: opsdesk
  labels: { app: opsdesk-api }
spec:
  selector: { app: opsdesk-api }
  ports:
    - name: http
      port: 80
      targetPort: 8000
